# Secrets — never version these
.env
.env.*
!.env.example
*.key
*.pem

# Dependencies and build output
node_modules/
.tools/
out/
cache/
dist/
build/
.next/
next-env.d.ts
lib/forge-std/

# Deployment records. The addresses the site reads live in
# web/lib/deployment.json, which IS versioned; these are per-run logs that may
# sit next to keys.
script/deployments/

# Private working material. Product architecture and launch requirements live
# in the versioned docs/ directory so a clean clone remains understandable.
DECISIONS.md
notes/
*-NOTES.md
.archive/

# Editor and tooling configuration, including files some frameworks regenerate
# on every run
CLAUDE.md
AGENTS.md
GEMINI.md
.claude/
.cursor/
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
*.log
*.log.*

# TypeScript incremental build state
*.tsbuildinfo

.vercel
.env.local
script/deployments/paymaster-monitor-state.json
